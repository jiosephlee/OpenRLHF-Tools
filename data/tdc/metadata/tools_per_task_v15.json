{
  "__default__": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "AMES": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task AMES. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "BBB_Martins": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task BBB_Martins. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "Bioavailability_Ma": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task Bioavailability_Ma. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "CYP2C9_Substrate_CarbonMangels": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task CYP2C9_Substrate_CarbonMangels. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "CYP2D6_Substrate_CarbonMangels": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task CYP2D6_Substrate_CarbonMangels. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "CYP3A4_Substrate_CarbonMangels": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task CYP3A4_Substrate_CarbonMangels. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "Carcinogens_Lagunin": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task Carcinogens_Lagunin. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "ClinTox": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task ClinTox. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "DILI": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task DILI. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "HIA_Hou": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task HIA_Hou. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "PAMPA_NCATS": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task PAMPA_NCATS. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "Pgp_Broccatelli": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task Pgp_Broccatelli. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "SARSCoV2_3CLPro_Diamond": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task SARSCoV2_3CLPro_Diamond. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "SARSCoV2_Vitro_Touret": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task SARSCoV2_Vitro_Touret. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "Skin_Reaction": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task Skin_Reaction. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ],
  "hERG": [
    {
      "type": "function",
      "function": {
        "name": "get_mol_properties_and_fg",
        "description": "Return plain-text single-molecule property evidence. The tool outputs one line per dense property in 'display_name: value' form for the 36 default dense properties, followed by the molecule's present functional groups and their counts. When the strongest acidic pKa or strongest basic pKa is undefined, the text explicitly says 'not applicable (no acidic/basic site)'.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    },
    {
      "type": "function",
      "function": {
        "name": "compare_similar_mols",
        "description": "Retrieve text-form local analog evidence for task hERG. The input SMILES must belong to that task dataset so the tool can identify the query split and retrieve the nearest training-set neighbors. The tool returns plain text with a short definition of query/neighbor/delta, then positive and negative neighbors (typically 3 positive and 3 negative, depending on the active manifest), each with neighbor/query/delta values for the 36 dense properties plus functional-group differences.",
        "parameters": {
          "type": "object",
          "properties": {
            "smiles": {
              "type": "string",
              "description": "SMILES string for the molecule to analyze."
            }
          },
          "required": [
            "smiles"
          ],
          "additionalProperties": false
        },
        "strict": true
      }
    }
  ]
}